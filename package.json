{
  "name": "agent-kit",
  "version": "0.1.6",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "test": "node --test",
    "validate": "node scripts/validate.mjs",
    "check": "node scripts/validate.mjs && node --test",
    "kit": "node bin/agent-kit.mjs"
  }
}
