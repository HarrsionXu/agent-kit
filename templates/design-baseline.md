# 项目 UI / UX 设计基线

这是可填写模板，不是已确认的设计系统。在业务工程现有设计文档维护唯一正文；没有时复制到项目自有 docs 目录，并在项目事实或适用 AGENTS 中登记。不要修改 `.agent-kit/templates/` 中的受管理副本，也不要把业务数据写回中央 Kit。

## 适用范围与状态

- 产品 / 端 / 路径：
- 状态（候选 / 部分确认 / 已确认）、版本或日期：
- 用户角色、核心任务、设备与使用环境：
- 信息密度及选择依据：
- 确认依据与待确认项（无记录时明确待确认）：

## 权威定义

引用现有定义，避免复制相互漂移的数值表。新项目尚未确定的 token 先记录为候选。

| 内容 | 权威文件或代码路径 | 适用范围 / 使用约定 / 已知限制 |
| --- | --- | --- |
| 颜色、字体、间距、圆角与其他 token | | |
| 基础组件及表单/表格/弹层 | | |
| 交互、状态文案与可访问性要求 | | |

## 页面模式与认可样板

| 模式 / 用户任务 | 页面或组件路径 / 路由 | 认可部分与不应沿用部分 | 截图、视口、数据状态 | 确认依据与日期 |
| --- | --- | --- | --- | --- |
| 列表：查找、比较、进入操作 | | | | |
| 详情：理解、追溯、进入编辑 | | | | |
| 编辑：填写、校验、保存、取消 | | | | |

按实际产品增删模式；没有适用样板时标记候选，不强制凑满三个页面。明确主要动作、筛选/分页/返回、只读与编辑、弹窗与抽屉的选择依据。

## 状态与验证

- 适用状态及呈现/恢复约定（加载、空数据、无匹配、失败、只读、无权限、成功）：
- 核心路径、上下文保留、取消与未保存变更的处理：
- 键盘/焦点、长内容与目标视口的检查方法：
- 已有自动检查入口、浏览器验证方式及证据位置：
- 尚未验证的部分：

## 例外与变更

| 范围 | 偏离哪条规则 | 原因 / 依据 | 确认记录 | 复查条件 |
| --- | --- | --- | --- | --- |

新方案不会自动覆盖基线。确认后记录更新内容与受影响页面模式，不以基线更新为由批量重写历史页面。
